name: CI — Web

# Path filtering happens at the JOB level (via the `changes` job), never with
# workflow-level `on.paths`/`paths-ignore`. Lint/Test/Build are REQUIRED status
# checks on main: a workflow that never triggers reports nothing and leaves the
# PR stuck on "Expected — waiting for status", whereas a job skipped by an
# `if:` condition reports "skipped", which branch protection counts as passing.

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-web-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: web

jobs:
  changes:
    name: Detect web changes
    runs-on: ubuntu-latest
    outputs:
      web: ${{ steps.filter.outputs.web }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            web:
              - 'web/**'
              - '.github/workflows/ci-web.yml'

  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.web == 'true'
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run format:check
      # The design-sync barrel (web/.design-sync/entry.jsx) re-exports app
      # components by name for the claude.ai/design bundle. Nothing else
      # imports it, so a rename or deletion in web/src breaks it silently —
      # see web/.design-sync/NOTES.md. Bundling it here is the control.
      - run: npm run check:design-sync
      # A raw colour hex in a view is pinned to whichever theme it was written
      # for and looks correct there, so review never catches it. #287 shipped
      # the light flip claiming zero literals remained; 66 did, and the ones on
      # the overview painted white text onto a white tile. Nine screenshots
      # cannot cover that class — this does.
      - run: npm run check:colours
      # Scoped to production dependencies: dev-only transitive advisories
      # (eslint/vitest/workbox tooling) must not block application PRs. An
      # unscoped gate deadlocked the repo for a month in 2026-08 — it failed
      # every web PR, including the Dependabot PRs carrying the fixes, and no
      # single bump could clear it. Anything reaching users is still gated.
      - run: npm audit --omit=dev --audit-level=high

  test:
    name: Test & Coverage
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.web == 'true'
    permissions:
      contents: read
      # OIDC for the Sentry Prevent upload below — avoids storing a token.
      id-token: write
    steps:
      - uses: actions/checkout@v7
        with:
          # Prevent needs full history to attribute results to commits.
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
      - run: npx vitest run --coverage
      - uses: davelosert/vitest-coverage-report-action@v2
        if: always()
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          json-summary-path: web/coverage/coverage-summary.json
          json-final-path: web/coverage/coverage-final.json
      # Sentry Test Analytics: flaky-test detection and per-test failure
      # history, which the coverage comment above cannot show. `if: always()`
      # because a FAILING run is exactly the one worth uploading.
      #
      # fail_ci_if_error is false deliberately: this is a reporting sink, not
      # a quality gate. An outage or an unprovisioned Prevent account must not
      # block a PR whose tests actually passed. Only test results are uploaded
      # — coverage already has a working reporter above.
      - name: Upload test results to Sentry Test Analytics
        if: always()
        uses: getsentry/prevent-action@v0
        with:
          files: ./web/test-results/junit.xml
          flags: unittests
          name: vitest
          fail_ci_if_error: false

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [changes, test]
    if: needs.changes.outputs.web == 'true'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
      - run: npm run build
      # The baseline is the bundle-size.json saved by the most recent push to
      # main (see the save steps below); restore-keys falls back to the newest
      # one when the exact base SHA has no cache entry.
      - name: Restore bundle-size baseline
        if: github.event_name == 'pull_request'
        uses: actions/cache/restore@v6
        with:
          path: web/.bundle-size-baseline.json
          key: bundle-size-baseline-${{ github.event.pull_request.base.sha }}
          restore-keys: bundle-size-baseline-
      - name: Bundle-size budget
        run: npm run size
      # Report even when the budget gate fails — the delta is most useful then.
      - name: Bundle-size report
        if: ${{ !cancelled() && github.event_name == 'pull_request' }}
        run: |
          node scripts/report-bundle-size.mjs .bundle-size-baseline.json > bundle-size-report.md
          cat bundle-size-report.md >> "$GITHUB_STEP_SUMMARY"
      - name: Comment bundle size on PR
        if: ${{ !cancelled() && github.event_name == 'pull_request' }}
        uses: actions/github-script@v9
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('web/bundle-size-report.md', 'utf8');
            const marker = '<!-- bundle-size-report -->';
            const { data: comments } = await github.rest.issues.listComments({
              ...context.repo,
              issue_number: context.issue.number,
              per_page: 100,
            });
            const existing = comments.find((c) => c.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                ...context.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                ...context.repo,
                issue_number: context.issue.number,
                body,
              });
            }
      - name: Save bundle-size baseline (main)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: cp dist/bundle-size.json .bundle-size-baseline.json
      - name: Cache bundle-size baseline (main)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/cache/save@v6
        with:
          path: web/.bundle-size-baseline.json
          key: bundle-size-baseline-${{ github.sha }}
